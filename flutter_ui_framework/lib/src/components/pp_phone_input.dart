import 'package:flutter/material.dart';

class PPPhoneInput extends StatelessWidget {
  const PPPhoneInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
