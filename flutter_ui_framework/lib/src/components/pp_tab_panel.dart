import 'package:flutter/material.dart';

class PPTabPanel extends StatelessWidget {
  const PPTabPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
