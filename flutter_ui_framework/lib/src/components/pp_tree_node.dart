import 'package:flutter/material.dart';

class PPTreeNode extends StatelessWidget {
  const PPTreeNode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
