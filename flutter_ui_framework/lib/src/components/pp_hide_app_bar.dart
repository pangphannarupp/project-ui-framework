import 'package:flutter/material.dart';

class PPHideAppBar extends StatelessWidget {
  const PPHideAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
