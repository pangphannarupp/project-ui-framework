import 'package:flutter/material.dart';

class PPBreadcrumb extends StatelessWidget {
  const PPBreadcrumb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
