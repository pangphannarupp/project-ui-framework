import 'package:flutter/material.dart';

class PPRadio extends StatelessWidget {
  const PPRadio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
