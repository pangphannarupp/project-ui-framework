import 'package:flutter/material.dart';

class PPInput extends StatelessWidget {
  const PPInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
