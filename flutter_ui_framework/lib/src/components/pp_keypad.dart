import 'package:flutter/material.dart';

class PPKeypad extends StatelessWidget {
  const PPKeypad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
