import 'package:flutter/material.dart';

class PPMagnifier extends StatelessWidget {
  const PPMagnifier({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
