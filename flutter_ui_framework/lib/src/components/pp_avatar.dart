import 'package:flutter/material.dart';

class PPAvatar extends StatelessWidget {
  const PPAvatar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
