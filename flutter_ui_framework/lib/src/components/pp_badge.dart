import 'package:flutter/material.dart';

class PPBadge extends StatelessWidget {
  const PPBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
