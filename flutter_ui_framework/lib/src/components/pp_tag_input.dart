import 'package:flutter/material.dart';

class PPTagInput extends StatelessWidget {
  const PPTagInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
