import 'package:flutter/material.dart';

class PPUserCard extends StatelessWidget {
  const PPUserCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
