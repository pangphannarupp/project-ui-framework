import 'package:flutter/material.dart';

class PPTable extends StatelessWidget {
  const PPTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
