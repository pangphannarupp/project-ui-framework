import 'package:flutter/material.dart';

class PPIconButton extends StatelessWidget {
  const PPIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
