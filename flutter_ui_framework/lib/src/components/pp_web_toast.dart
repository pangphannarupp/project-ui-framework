import 'package:flutter/material.dart';

class PPWebToast extends StatelessWidget {
  const PPWebToast({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
