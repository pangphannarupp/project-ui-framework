import 'package:flutter/material.dart';

class PPLineChart extends StatelessWidget {
  const PPLineChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
