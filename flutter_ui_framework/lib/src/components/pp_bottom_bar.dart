import 'package:flutter/material.dart';

class PPBottomBar extends StatelessWidget {
  const PPBottomBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
