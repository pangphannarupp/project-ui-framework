import 'package:flutter/material.dart';

class PPSlider extends StatelessWidget {
  const PPSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
