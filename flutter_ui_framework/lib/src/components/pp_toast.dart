import 'package:flutter/material.dart';

class PPToast extends StatelessWidget {
  const PPToast({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
