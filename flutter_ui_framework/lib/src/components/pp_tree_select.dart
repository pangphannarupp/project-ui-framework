import 'package:flutter/material.dart';

class PPTreeSelect extends StatelessWidget {
  const PPTreeSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
