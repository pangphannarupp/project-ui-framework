import 'package:flutter/material.dart';

class PPTree extends StatelessWidget {
  const PPTree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
