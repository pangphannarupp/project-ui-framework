import 'package:flutter/material.dart';

class PPSwipeItem extends StatelessWidget {
  const PPSwipeItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
