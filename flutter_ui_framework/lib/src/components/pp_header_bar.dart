import 'package:flutter/material.dart';

class PPHeaderBar extends StatelessWidget {
  const PPHeaderBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
