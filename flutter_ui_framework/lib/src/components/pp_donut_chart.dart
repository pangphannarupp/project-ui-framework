import 'package:flutter/material.dart';

class PPDonutChart extends StatelessWidget {
  const PPDonutChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
