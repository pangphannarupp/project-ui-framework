import 'package:flutter/material.dart';

class PPOtpInput extends StatelessWidget {
  const PPOtpInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
