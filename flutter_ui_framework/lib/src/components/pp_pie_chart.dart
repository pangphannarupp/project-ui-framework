import 'package:flutter/material.dart';

class PPPieChart extends StatelessWidget {
  const PPPieChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
