import 'package:flutter/material.dart';

class PPStepper extends StatelessWidget {
  const PPStepper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
