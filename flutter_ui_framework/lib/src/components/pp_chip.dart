import 'package:flutter/material.dart';

class PPChip extends StatelessWidget {
  const PPChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
