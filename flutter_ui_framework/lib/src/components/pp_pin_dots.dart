import 'package:flutter/material.dart';

class PPPinDots extends StatelessWidget {
  const PPPinDots({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
