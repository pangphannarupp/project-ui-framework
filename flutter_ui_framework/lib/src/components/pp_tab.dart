import 'package:flutter/material.dart';

class PPTab extends StatelessWidget {
  const PPTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
