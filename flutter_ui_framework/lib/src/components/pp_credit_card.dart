import 'package:flutter/material.dart';

class PPCreditCard extends StatelessWidget {
  const PPCreditCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
