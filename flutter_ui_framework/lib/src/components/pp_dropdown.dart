import 'package:flutter/material.dart';

class PPDropdown extends StatelessWidget {
  const PPDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
