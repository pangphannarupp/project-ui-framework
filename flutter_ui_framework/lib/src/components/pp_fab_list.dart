import 'package:flutter/material.dart';

class PPFabList extends StatelessWidget {
  const PPFabList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
