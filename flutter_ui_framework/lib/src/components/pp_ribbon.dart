import 'package:flutter/material.dart';

class PPRibbon extends StatelessWidget {
  const PPRibbon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
