import 'package:flutter/material.dart';

class PPMarquee extends StatelessWidget {
  const PPMarquee({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
