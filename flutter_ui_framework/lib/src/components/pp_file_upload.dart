import 'package:flutter/material.dart';

class PPFileUpload extends StatelessWidget {
  const PPFileUpload({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
