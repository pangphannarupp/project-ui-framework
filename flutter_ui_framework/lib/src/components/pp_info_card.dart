import 'package:flutter/material.dart';

class PPInfoCard extends StatelessWidget {
  const PPInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
