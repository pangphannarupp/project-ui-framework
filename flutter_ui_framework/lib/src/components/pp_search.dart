import 'package:flutter/material.dart';

class PPSearch extends StatelessWidget {
  const PPSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
