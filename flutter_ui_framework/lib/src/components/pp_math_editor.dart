import 'package:flutter/material.dart';

class PPMathEditor extends StatelessWidget {
  const PPMathEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
