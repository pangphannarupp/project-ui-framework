import 'package:flutter/material.dart';

class PPNoResult extends StatelessWidget {
  const PPNoResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
