import 'package:flutter/material.dart';

class PPQRCode extends StatelessWidget {
  const PPQRCode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
