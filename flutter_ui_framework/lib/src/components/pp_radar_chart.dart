import 'package:flutter/material.dart';

class PPRadarChart extends StatelessWidget {
  const PPRadarChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
