import 'package:flutter/material.dart';

class PPSkeleton extends StatelessWidget {
  const PPSkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
