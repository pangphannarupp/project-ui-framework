import 'package:flutter/material.dart';

class PPTabs extends StatelessWidget {
  const PPTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
