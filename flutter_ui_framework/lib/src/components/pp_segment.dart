import 'package:flutter/material.dart';

class PPSegment extends StatelessWidget {
  const PPSegment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
