import 'package:flutter/material.dart';

class PPCarousel extends StatelessWidget {
  const PPCarousel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
