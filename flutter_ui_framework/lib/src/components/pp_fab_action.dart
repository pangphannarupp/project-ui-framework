import 'package:flutter/material.dart';

class PPFabAction extends StatelessWidget {
  const PPFabAction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
