import 'package:flutter/material.dart';

class PPPdfViewer extends StatelessWidget {
  const PPPdfViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
