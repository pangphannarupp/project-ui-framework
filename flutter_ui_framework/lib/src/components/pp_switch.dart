import 'package:flutter/material.dart';

class PPSwitch extends StatelessWidget {
  const PPSwitch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
