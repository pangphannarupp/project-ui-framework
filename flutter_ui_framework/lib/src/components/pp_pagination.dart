import 'package:flutter/material.dart';

class PPPagination extends StatelessWidget {
  const PPPagination({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
