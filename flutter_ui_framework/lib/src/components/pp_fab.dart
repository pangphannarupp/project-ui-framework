import 'package:flutter/material.dart';

class PPFab extends StatelessWidget {
  const PPFab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
