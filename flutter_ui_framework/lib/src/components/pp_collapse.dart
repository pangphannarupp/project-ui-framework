import 'package:flutter/material.dart';

class PPCollapse extends StatelessWidget {
  const PPCollapse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
