import 'package:flutter/material.dart';

class PPOnboard extends StatelessWidget {
  const PPOnboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
