import 'package:flutter/material.dart';

class PPCheckbox extends StatelessWidget {
  const PPCheckbox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
