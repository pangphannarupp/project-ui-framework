import 'package:flutter/material.dart';

class PPSpinWheel extends StatelessWidget {
  const PPSpinWheel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 50, fallbackWidth: 100);
  }
}
